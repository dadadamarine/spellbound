# SPELLBOUND 게임 목표 계약

원문 요구사항은 [[게임_기획_원문.md]]에 수정 없이 보존한다.

후속 기능 고도화안은 [GAME_DESIGN_V2.md](GAME_DESIGN_V2.md)에 정리한다.

## Surface goal

기존 SPELLBOUND 단일 HTML 게임을 탑뷰 단어 수집 게임으로 변형한다. 플레이어는 월드를 탐험하며 문맥 단서만으로 단어를 포획해 덱을 만들고, 시험으로 실패를 복구하며, 카드정리소에서 덱을 정리하고, 명예의 전당에서 비슷한 티어의 상대와 대전해 랭킹을 올린다.

## Initial GQM

### Goal

별도 설치 없이 `index.html`을 열어 탐험 → 단어 조우 → 포획/복구 시험 → 덱 관리 → 랭크 대전의 핵심 루프를 끝까지 플레이할 수 있는 프로토타입을 완성한다.

### Questions

1. 플레이어가 맵을 자유롭게 탐험하고 풀숲과 두 핵심 시설에 도달할 수 있는가?
2. 정답과 오답 양쪽에서 단어 포획 규칙이 정확히 작동하고 덱 상태가 보존되는가?
3. 카드정리소와 명예의 전당이 덱·시험·랭킹 규칙을 일관되게 적용하는가?

### Metrics / Evidence

- 순수 규칙 함수 자동 테스트: 충돌, 90% 합격선, 덱 중복 방지, 제거 조건, 대전 점수와 랭킹 변동
- 브라우저 E2E: 시작부터 각 시설 및 단어 조우의 대표 성공·실패 경로 실행
- 새로고침 전후 `localStorage` 진행 상태 비교
- 데스크톱·모바일 렌더링 확인과 브라우저 console error 0건
- 변경 범위: `index.html`, 게임 목표/원문 문서, 전용 테스트 파일

### Checklist

- [x] 38×28 탑뷰 월드맵에서 키보드·화면 버튼 이동, 충돌, 카메라, 지역 전환이 작동한다.
- [x] 단어 연구소·카드정리소·명예의 전당·영어박사님 집이 맵에 표시되고 입구를 조사할 수 있다.
- [x] 영어박사님 집에서 25장 스타팅 덱 3개 중 하나를 고르고, 무작위 5장 시험에서 4장 이상 맞혀야 획득한다.
- [x] 풀숲을 걷다 확률적으로 단어 조우가 발생한다.
- [x] 조우 화면은 정답 철자를 노출하지 않고 뜻과 목표 단어가 빈칸인 예문만 보여준다.
- [x] 첫 문제 정답 시 해당 언어의 단어 카드가 덱에 중복 없이 추가된다.
- [x] 첫 문제 오답 시 덱에서 최대 10장을 임의 추출한 복구 시험이 열리고 90% 이상이면 목표 단어를 포획한다.
- [x] 덱이 비어 있어 복구 시험을 만들 수 없는 경우를 설명하고 안전하게 맵으로 돌아간다.
- [x] 덱·레이팅·랭킹은 새로고침 후에도 유지된다.
- [x] 카드정리소는 덱 50장 이상에서만 5장 시험을 시작한다.
- [x] `[가설]` 카드정리소 시험은 4/5 이상을 통과로 보고, 맞힌 카드 중 선택한 1장만 제거한다.
- [x] 명예의 전당은 덱에서 무작위 손패 5장을 만들고 서로 단어를 제시·응답하는 5라운드 대전을 진행한다.
- [x] 승리 시 레이팅이 오르고 패배 시 내려가며 티어 표시가 함께 갱신된다.
- [x] `[가설]` 단일 HTML 프로토타입에서는 실제 사용자 대신 현재 레이팅과 비슷한 `MOCK USER`를 매칭한다. 실제 사용자 매칭은 서버·계정·동기화가 포함된 후속 제품 게이트다.
- [x] 자동 테스트와 실제 브라우저 대표 경로가 통과하고 console error가 없다.
- [x] 각 활성 root-cause leaf에 더 깊은 증거 기반 actionable 원인이 남지 않았거나 잔여 리스크가 기록됐다.

## 규칙 가정

- 포획 시험 문제 수: `min(10, 현재 덱 카드 수)`
- 포획 시험 합격선: `ceil(문제 수 × 0.9)`
- 스타팅 덱: 서로 겹치지 않는 Tier 1 카드 25장 × 3종
- 스타팅 덱 시험: 선택한 덱의 20%인 5문제, 합격선 4/5
- 한 덱을 고르면 시험 통과 전까지 다른 덱은 잠기며, 통과 후 해당 덱 확정 또는 포기 후 재선택
- 카드 고유 키: `언어:표제어`
- 카드정리소 시작 조건: 시험 시작 시점의 덱이 50장 이상
- 예문: 외부 백과사전 문장을 그대로 복제하지 않고 프로토타입용 자체 문장 템플릿을 사용한다. 외부 데이터 도입 시 출처·라이선스를 카드 데이터와 함께 기록한다.
- 대전: 5라운드 종료 시 정답 수로 승패를 정하고 동점은 레이팅을 유지한다.
- MOCK 상대 응답: User 구조체의 `answerPattern`에 따라 1라운드는 오답, 2~5라운드는 정답으로 고정한다.
- 사용 언어: 현재 제품 UI와 런타임은 영어 전용이며 영어 단어 500개만 사용한다.

## Evidence log

- 맵 단위 테스트 4/4 통과.
- 단어 수집 규칙 테스트 5/5 통과: 빈칸 예문, 합격선, 중복 방지, 조우 조건, 악센트 판정.
- 브라우저에서 시작점 → 다리 → 기억의 숲 → 명예의 전당 이동과 건물 조사를 확인.
- 브라우저에서 빈 덱 오답, 정답 포획, 보유 덱 복구 시험 통과, 덱 2장 반영을 확인.
- 포획 후 새로고침해 덱 카드 수가 유지되는 것을 확인.
- 카드정리소 규칙 테스트 4/4 통과: 50장 게이트, 4/5 합격선, 정답 카드 필터, 불변 1장 제거.
- 브라우저에서 카드정리소 입구 조사와 덱 2장일 때 `48장 부족` 차단 안내를 확인.
- 명예의 전당 규칙 테스트 4/4 통과: 5장 입장 조건, 승무패, ±25 레이팅, ±40 상대 매칭.
- 브라우저에서 카드 5장을 실제 포획한 뒤 5라운드 대전을 `5:4`로 완료하고 레이팅 `1000 → 1025`, 전적 `1승` 반영을 확인.
- 새로고침 후 덱 5장·레이팅 1025·전적 1승 유지 확인.
- User 구조체와 MOCK 응답 패턴 테스트를 추가했다.
- 브라우저에서 `MOCK USER` 표시, 1라운드 상대 오답, 2라운드 상대 정답을 확인. 모바일 가로 넘침 없음, 최종 브라우저 console error 0건.
- 영어 전용 설정·런타임 테스트를 추가해 전체 자동 테스트 20/20 통과.
- 브라우저 설정에서 영어 500단어만 노출되고 스페인어 선택지가 없으며, 맵 수집량이 `5 / 500`으로 표시되는 것을 확인.

## Residual risks

- 실제 사용자 간 매칭·동시 대전은 백엔드, 계정, 매치메이킹, 부정행위 방지가 필요하므로 현재 단일 HTML 범위에는 포함하지 않았다. 화면에는 `MOCK USER`임을 명시했다.
- 카드정리소의 50장 미만 차단 경로는 실제 브라우저로 검증했고, 50장 시험·선택 제거 규칙은 자동 테스트로 검증했다. 브라우저 저장 데이터가 5장이라 50장 성공 경로를 수동 실행하지는 않았다.

## 5 Whys / Root-Cause Log

- 관찰: 단어 수집 로직 첫 구문 검사에서 문자열 조립 구문 오류가 발생했다.
  - Why: 인라인 `onclick`/`onkeydown`을 단일 인용부호 JavaScript 문자열 안에서 다시 이스케이프했다.
  - Why: HTML과 JavaScript 두 인용 문맥을 한 문자열에서 동시에 관리해 구문 경계가 불명확했다.
  - Actionable root: Enter 제출 핸들러를 생성 문자열에서 제거하고 DOM 생성 후 함수로 연결했다. 전체 로직 블록을 `vm.Script`로 파싱하는 테스트가 같은 유형의 회귀를 차단한다.
  - Terminal reason: 남은 경로는 정적 악센트 버튼 문자만 제한 목록에서 생성하며 현재 구문·브라우저 검증을 통과했다.
- 관찰: 브라우저 검증 중 두 번째 조우가 예상 밖으로 오답 제출됐다.
  - Why: 공유 Chrome 탭에 사용자 입력이 동시에 들어갔다.
  - Evidence: 같은 화면을 다시 열어 무입력 상태로 5초 대기했을 때 입력창과 패널이 유지됐다.
  - Terminal reason: 앱 내부 자동 제출 경로는 재현되지 않았고 동시 입력은 외부 상호작용이므로 코드 수정 대상이 아니다.

## Root-Cause Depth Check

- [x] 활성 관찰·실패·가설마다 더 깊은 actionable Why가 남았는지 확인했다.
- [x] 더 깊은 원인은 계약과 테스트에 반영했고, 외부 시스템이 필요한 불확실성은 잔여 리스크로 기록했다.

## Completion gate

위 체크리스트가 각각 관찰 가능한 증거와 함께 통과하고 Root-Cause Depth Check가 닫혀야 목표가 완료다.

---

## 스토리 도입부 구현 목표 (2026-08-19)

### Goal

영어박사님의 편지 프롤로그부터 스타팅 덱, 신규 단어 3장 포획, 새싹마을 표지판 복구, 백지단 등장, 1번 길 해금, 첫 라이벌 비랭크 대결까지를 중복 없이 저장되는 하나의 플레이 흐름으로 구현한다.

### Questions

1. 신규 사용자가 프롤로그와 25장 덱 시험을 거쳐 첫 임무를 받는가?
2. 중복 카드를 제외한 신규 포획만 3장까지 카운트되고 표지판이 단계적으로 복구되는가?
3. 백지단 이벤트 전후로 1번 길 장벽이 정확히 열리고, 라이벌 대결 후 다시 막히지 않는가?
4. 첫 라이벌전이 기존 5라운드 대결 규칙을 쓰면서도 랭크 점수를 변경하지 않는가?
5. 7초 제한시간이 숫자와 연속 감소 게이지로 명확히 보이는가?

### Completion checklist

- [x] 편지→사라진 마을 이름→영어박사님의 부탁 3장면 프롤로그를 추가했다.
- [x] 프롤로그를 본 뒤에만 기존 25장 스타팅 덱 선택으로 이어진다.
- [x] 덱 확정 즉시 `capture_first_three` 임무가 0/3으로 시작한다.
- [x] 새싹마을 튜토리얼 풀숲에서 신규 카드만 임무 진행도로 계산한다.
- [x] 0/3부터 3/3까지 마을 표지판 문구가 단계적으로 복구된다.
- [x] 3번째 포획 후 백지단 장면이 열리고 확인 후에만 1번 길 장벽이 사라진다.
- [x] 1번 길의 라이벌이 진행을 막고 5라운드 비랭크전을 제공한다.
- [x] 라이벌은 기존 MockUser 규칙대로 1라운드만 틀리고 2~5라운드를 맞힌다.
- [x] 라이벌전 승·패·무와 관계없이 스토리가 진행하고 레이팅과 전적은 변하지 않는다.
- [x] 스토리 상태를 progress version 6에 저장하고, 기존 저장 사용자는 새 장벽으로 막히지 않게 마이그레이션한다.
- [x] 단어 입력 타이머를 7초 숫자+연속 감소 게이지로 표시한다.
- [x] 순수 규칙·회귀 테스트와 데스크톱 브라우저 E2E를 통과했다.

### Evidence

- `node --test test_*.mjs`: 51/51 통과.
- 신규 브라우저 오리진에서 프롤로그→생활 덱 5/5 시험→25장 확정→`field`, `quiet`, `picture` 포획→28장→백지단→라이벌전을 실행했다.
- 라이벌전은 사용자 5 : 4 라이벌 승리로 종료했고 레이팅은 `1000 → 1000`으로 유지됐다.
- 새로고침 전후 스토리 상태는 `enter_memory_forest`, 목표는 “1번 길 너머 기억의 숲으로 들어가세요.”로 유지됐고 중복 패널은 열리지 않았다.
- 타이머 게이지는 1.5초 경과 후 `77.1143%`로 줄어든 DOM과 렌더링을 확인했다.
- 최종 브라우저 console warning/error 0건.

### 5 Whys / Root-Cause Log

- 관찰: 초기 맵의 풀숲은 1번 길 너머 기억의 숲에만 있었다.
  - Why: 스토리 장벽을 다리에 추가하면 장벽 해제 조건인 첫 3장 포획에 도달할 수 없었다.
  - Actionable root: 새싹마을 안에 튜토리얼 풀숲을 추가하고 `capture_first_three` 임무 중에는 조우를 보장했다.
  - Terminal reason: 해금 전에도 도달 가능한 풀숲에서 실제 3장 포획을 완료했다.
- 관찰: 숫자 초 표시만으로는 남은 시간의 흐름을 즉시 인지하기 어려웠다.
  - Why: 표시가 1초 단위 정수로만 바뀌어 시간 소모가 단절되어 보였다.
  - Actionable root: 200ms마다 남은 시간 비율을 계산해 감소하는 게이지와 경고·위험 색상을 추가했다.
  - Terminal reason: 스타팅 시험 화면에서 숫자와 게이지가 같이 감소하는 것을 화면과 DOM으로 확인했다.

### Residual risks

- 이번 목표는 기억의 숲 진입 직전까지의 스토리 도입부다. 숲 이후 퀘스트와 백지단 본편은 후속 범위다.
- 첫 라이벌전은 공유 User 구조와 MockUser 응답 규칙을 사용한다. 실제 사용자 매칭은 서버·계정·동기화가 필요한 후속 목표다.

### Root-Cause Depth Check

- [x] 진행 불가를 만드는 순환 의존성을 튜토리얼 풀숲으로 해소했다.
- [x] 타이머 인지성 요구를 연속 시간 비율 표시로 검증했다.
- [x] 저장·중복 트리거·기존 사용자 마이그레이션을 각각 테스트로 닫았다.
